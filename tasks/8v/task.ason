{
	title: 'Set up trusted publishing for future npm releases',
	description: 'Set up token-free npm trusted publishing from GitHub Actions using OIDC after the first manual release. Document npm-side setup and verify the workflow on a future release.',
	status: 'planned',
	notes: 'GitHub Actions workflow: .github/workflows/publish.yml (not verified). On npmjs.com, add a trusted publisher for anttikissa/tsk with workflow filename publish.yml, no environment, and direct npm publish allowed. Verify an authorized new release, then copy the working GitHub workflow YAML into this task as a supporting file for rebuilds.',
	needs: ['m4']
}
