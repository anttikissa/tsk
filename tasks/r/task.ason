{
	title: 'Project discovery and task loading',
	description: 'From a working directory, find the nearest Git root and load its tasks/ only if project.ason identifies the Tsk format. Read task directories and their ASON records, deriving IDs from directory names. Accept optional boolean once and a list of string notes; reject malformed fields, invalid status, missing dependencies, and cycles with useful errors. Never silently adopt an unrelated tasks/ directory.',
	status: 'done',
	needs: ['9']
}
