{
	title: 'Show task artifacts',
	description: 'tsk show <id> includes an artifacts list of files in the task directory other than its root task.ason. Include nested files as relative paths in stable order; include symlinks without following them and use an empty list when there are no artifacts.',
	status: 'done',
	needs: ['5p']
}
